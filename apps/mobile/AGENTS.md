# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This project is pinned to SDK 54 (not the latest) because the App Store build of Expo Go
only supports SDK 54 — newer Expo Go clients are simulator/TestFlight-only. Do not upgrade
the `expo` package past ^54 while the team previews on physical iPhones via Expo Go.
