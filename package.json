{
  "name": "coco",
  "private": true,
  "scripts": {
    "dev:backend": "docker compose up backend postgres redis",
    "dev:backend:offline": "AI_PROVIDER=local docker compose up backend postgres redis",
    "ai:offline": "scripts/offline-ai.sh",
    "dev:caretaker": "pnpm --filter caretaker-dashboard dev",
    "dev:admin": "pnpm --filter admin-dashboard dev",
    "dev:dashboards": "pnpm --parallel --filter caretaker-dashboard --filter admin-dashboard dev",
    "build:caretaker": "pnpm --filter caretaker-dashboard build",
    "build:admin": "pnpm --filter admin-dashboard build",
    "build:dashboards": "pnpm --filter caretaker-dashboard --filter admin-dashboard build"
  },
  "engines": {
    "node": ">=20"
  }
}
